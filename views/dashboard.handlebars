<div class="row">
  <div class="col-auto">
    <h2 class="welcome">Welcome, <span class="welcomeName">{{name}}</span></h2>
  </div>
</div>

<div class="row mt-4">
  <div class="col-md-6">
    <h3>Create a New Post!</h3>
    <form class="form new-post-form">
      <div class="form-group">
        <label for="post-name">Title:</label>
        <input class="form-input" type="text" id="post-name" name="post-name" />
      </div>
      <div class="form-group">
        <label for="post-desc">Content:</label>
        <input class="form-input" type="text" id="post-desc" name="post-desc" />
      </div>
      <div class="form-group ">
        <button type="submit" class="btn btn-warning">Create</button>
      </div>
    </form>
  </div>

  {{#if posts.length}}
  <div class="col-md-6 post-list">
    <h3>Current Posts:</h3>
    <div class="currentPostTitle">
      {{#each posts as |post|}}
    </div>
    <div class="row mb-2">
      <div class="col-md-8">
        <h4><a href="/post/{{post.id}}">{{post.title}}</a></h4>
      </div>
      <div class="col-md-4">
        <button class="btn btn-sm btn-danger" data-id="{{post.id}}">DELETE</button>
      </div>
    </div>
    {{/each}}
  </div>
  {{/if}}
</div>

  {{#if posts.length}}
    {{#each posts as |post|}}
      <h3 class="mt-5">Update an Existing Post:</h3>

      <form class="form updating-post-form">
        <input type="hidden" name="post-id" id="post-id" value="{{post.id}}" />
        <div class="form-group">
          <label for="update-title">Post Title:</label>
          <input class="form-input" type="text" id="update-title" name="update-title" value="{{post.title}}" />
        </div>
        <div class="form-group">
          <label for="update-content">Content:</label>
          <textarea class="form-input" id="update-content" name="update-content" rows="4">{{post.content}}</textarea>
        </div>
        <div class="form-group">
          <button class="btn btn-warning updatePostButton" data-id="{{post.id}}">Update</button>
        </div>
      </form>
    {{/each}}
  {{/if}}

<script src="/js/dashboard.js"></script>
